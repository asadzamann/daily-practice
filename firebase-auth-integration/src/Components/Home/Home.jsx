import React, { use } from 'react';
import { AuthContext } from '../Contexts/AuthContexts/AuthContext';

const Home = () => {
 
  
    return (
        <div>
            Home
        </div>
    );
};

export default Home;